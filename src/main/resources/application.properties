spring.application.name=CinemaBooking
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/cinema_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=tmd2601.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

spring.thymeleaf.cache=false

# Default: do not insert demo data from Java. Run database/seed-data.sql manually instead.
# Set app.seed.enabled=true only if you want to use DataSeeder again.
app.seed.enabled=false
